name: clinical-sp-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - fastapi
  - uvicorn
  - flask
  - python-dotenv
  - pydantic
  - pip:
      - langchain>=0.2
      - langchain-community>=0.2
      - chromadb>=0.5
      - pymilvus>=2.4
      - neo4j>=5.20
